import React from 'react';
import { NewModelFormContainer } from '../../NewModelForm';

export const NewConfiguredModelContent: React.FC = () => {
    return (
        <NewModelFormContainer />
    );
};
