type Props = {
    split_periods: string[];
    locations: string[];
}

export const BackTestFilter = ({ split_periods, locations }: Props) => {
    return (
        <div>
            Something here
        </div>
    );
}