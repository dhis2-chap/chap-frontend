---
'@dhis2-chap/modeling-app': patch
---

Add `pnpm e2e:chap-core` for running the e2e suite against a chosen chap-core version, and pull published chap-core images in the stable e2e workflow instead of building them from source
